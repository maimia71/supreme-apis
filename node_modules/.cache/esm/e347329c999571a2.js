let express,APP_PORT,routes;_999‍.w("express",[["default",["express"],function(v){express=v}]]);_999‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);_999‍.w("./routes/index",[["default",["routes"],function(v){routes=v}]]);





const app = express();

app.use('/api', routes);

app.listen(APP_PORT, (req, res) => {

    console.log(`server is running on ${APP_PORT}`);
})