let DEBUG_MODE,ValidationError,CustomErrorHandler;_d3e‍.x([["default",()=>_d3e‍.o]]);_d3e‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_d3e‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_d3e‍.w("../services/customErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);




// middleware takes four params
const errorHandler = (error, req, res, next) => {

    let statusCode = 500;

    let data = {
        message: 'Internal server error',
        ...(DEBUG_MODE === 'true' && { originalError: error.message })
    }

    if (error instanceof ValidationError) {

        statusCode = 422;

        data = {
            message: error.message
        }

    }

    if (error instanceof CustomErrorHandler) {
        statusCode = 409;

        data = {
            message: error.message
        }
    }

    return res.status(statusCode).json(data);

}

_d3e‍.d(errorHandler);